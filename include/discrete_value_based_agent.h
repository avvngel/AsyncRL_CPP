/**
 * @class DiscreteValueBasedAgent
 * @brief Implements an agent for discrete value-based decision processes.
 *
 * This agent class is designed to handle environments where the decision
 * space is discrete and decisions are made based on a value function.
 *
 * @tparam StateType The type of state used by the agent.
 * @tparam FunctionApproximatorType The type of function approximator.
 */

#pragma once

#include "agent.h"

template<StateType, ActionType, FunctionApproximatorType>
class DVBAgent { 
    
}
